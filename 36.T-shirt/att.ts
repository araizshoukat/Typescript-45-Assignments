function make_shirt(size:string, text:string){

    console.log(`Creating a ${size} shirt write the message: ${text}`);
}

make_shirt(`large`,`Hello From Los Angeles`);

make_shirt(`medium`,`Hello From Los Angeles`);

make_shirt(`small`,`Hello From Los Angeles`);